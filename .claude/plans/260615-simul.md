# Reimplement TPD-21 Simulations on freechains.vcs (TCP)

## Context

The `tpd-21` paper (Sec. consensus.eval) simulates chat/newsgroup
forums running over Freechains to show practicability.
Original sims live in `old/tpd-21/{chat,usenet}/` (this project) and
target the old Kotlin daemon (`freechains-host --port`, TCP `peer`
sync).

Goal: reimplement those sims against the current Lua/Git rewrite at
`/x/x/freechains/vcs/`, keeping the network model **TCP** and staying
as close as possible to the original driver.

Layout (moved 2026-06-15 into new project `/x/papers/26-06-vcs/`):
- `old/` is git-ignored and holds symlinks + datasets.
- `old/tpd-21` -> `/x/papers/tpd-21/` = paper repo with the sims.
  (`sbrc-23` is not on disk; `/x/papers/sbseg-23/` is unlinked.)
- `old/*.zip` = the four datasets, already fetched (see Datasets).
- `/x/x/freechains/vcs/` = reference impl, read-only, OUTSIDE this
  worktree (source reads + `freechains` CLI, already on PATH).
- Deliverable goes HERE, inside this worktree (see Steps 2).

## Source provenance (GitHub: fsantanna-no)

Same core paper, three venues:

| repo      | title                                            | sims?       |
|-----------|--------------------------------------------------|-------------|
| tpd-21    | P2P Permissionless Consensus via Authoring Rep.  | chat+usenet |
| sbrc-23   | P2P Permissionless Consensus via Authoring Rep.  | none        |
| sbseg-23  | P2P Permissionless Consensus via Reputation      | none        |

- Only `tpd-21` carries the simulation drivers; `sbseg-23` /
  `sbrc-23` are text-only condensations (no `chat/` or `usenet/`).
- `tpd-21` sim files: `chat/{chat-01..03.lua, chat-03.sh,
  common.lua}`, `usenet/{use-01..03.lua, common.lua}`.
- So this port has a single authoritative source: `tpd-21`.
- `chat/common.lua` and `usenet/common.lua` are byte-identical:
  port ONE shared harness, not two.

Supersedes the earlier scoping notes at
`/x/x/freechains/vcs/.work/06-10-sims/.claude/plans/06-10-sims.md`
(several "high/med risk" unknowns there are now resolved by reading
the vcs source — see mapping below).

## Verdict

Feasible.
The vcs DOES support TCP: `freechains daemon` wraps `git daemon`,
and `chain sync send/recv git://localhost:P/...` is the transport.
Methodology ports cleanly.
Absolute numbers will NOT match the paper (reps constants + CLI
changed); replication validates feasibility, not the figures.

## How the vcs TCP model works (verified in source)

- `freechains --root=R daemon --port=P --hub`
    - runs `git daemon --base-path=R/chains/ --export-all`
    - `--hub` adds `--enable=receive-pack` (accept pushes)
    - upload-pack stays on by default (accept fetches)
    - so one `--hub` daemon serves BOTH send and recv
    - src: `src/freechains.lua:215`
- `chain '#c' sync recv <url>`  = `git fetch` (pull)
    - src: `src/freechains/chain/sync.lua` (recv branch)
- `chain '#c' sync send <url>`  = `git push -o freechains=true`
    - remote `pre-receive` hook re-runs `sync recv` then rejects
      the push with "Freechains: OK" (expected)
    - hook auto-installed on `chains add` init+clone
      (`src/freechains/chains.lua:107,159`)
    - src: `src/freechains/hooks/pre-receive`
- URL builder turns `localhost:P` into `git://localhost:P/<alias>`
    - default PORT 8330; src: `src/freechains/common.lua:78` (URL)
- Identity = the SSH pubkey string (80 chars, `ssh-ed25519 <b64>`)
  extracted from the signed commit, NOT a file path.
    - so the same keyfile yields the same author across peers
    - src: `src/freechains/chain/ssh.lua` (M.pubkey / M.verify)
- Virtual time = `--now=<sec>` per command (`CMD.now`), which sets
  `GIT_AUTHOR_DATE`/`GIT_COMMITTER_DATE` and persists into `G.now`.
    - no daemon-held clock; monotonic time travels with commits
    - reps consolidation scans fire when `time > G.now`
    - src: `src/freechains.lua:205`, `chain/common.lua` (apply)

## Old CLI -> vcs CLI mapping (TCP)

| original (common.lua)              | vcs (TCP)                                              |
|------------------------------------|-------------------------------------------------------|
| `freechains-host --port=P start D` | `freechains --root=D daemon --port=P --hub &`         |
| `freechains-host --port=P stop`    | `pkill -f "git daemon.*--port=P"`                     |
| `now <ms>` (daemon clock)          | `--now=<sec>` on each post/like (no separate step)    |
| `keys pubpvt 'pass'`               | `ssh-keygen -t ed25519 -N '' -C '' -f /tmp/u/<user>`  |
| `chains join '$chat' SHA` pioneer  | `chains add '#chat' init inline --sign=<pioneerkey>`  |
| `chains join '$chat' SHA` others   | `chains add '#chat' clone git://localhost:PIO/#chat`  |
| `chain c post M P --sign=PVT`      | `--now=ts chain '#c' post inline P --sign=<keyfile>` (add `--beg` if 0-rep) |
| `chain c heads blocked`            | `chain '#c' list begs`                                |
| `chain c like HASH --sign=PVT`     | `--now=ts chain '#c' like <N> post HASH --sign=<keyfile>` |
| `chain c consensus`                | `chain '#c' list order`                               |
| `peer localhost:P send c` (push)   | `--root=SRC chain '#c' sync send git://localhost:P`   |
| `peer localhost:P recv c` (pull)   | `--root=SRC chain '#c' sync recv git://localhost:P`   |

Notes:
- `like`/`dislike` args = `<number> <target> <id>` (number is a
  positive int; target = `post`|`author`; src `freechains.lua:179`).
  So unblock = `like 1 post <hash>`.
- `reps` args = `<target> [key]`; targets `post|posts|author|authors`
  (src `chain/reps.lua`). Author key trimmed of whitespace.
- Author identity for "like author" needs the 80-char pubkey
  string, obtainable via `ssh-keygen -y -f <keyfile>`.
  `cat <keyfile>.pub` matches the key stored from a signed commit
  (both are the same ed25519 wire format -> identical base64).
- "Welcoming like on a blocked post" (the old unknown) = liking a
  `beg`: `list begs` yields parked post hashes on `refs/begs/`;
  `chain like 1 post <hash>` merges/unblocks it
  (`src/freechains/chain/like.lua`, `list.lua` begs branch).

## Beg/unblock mechanism (verified in source)

Key shift from the old system: a 0-rep plain post no longer
auto-blocks — it **errors** ("insufficient reputation"). The poster
must opt in with `--beg`, so the harness decides up front.

| step    | old Kotlin        | vcs (Lua/Git)                              |
|---------|-------------------|--------------------------------------------|
| 0-rep   | `post` -> BLOCKED | `post --beg --sign` -> commit parked on     |
| post    |                   | `refs/begs/beg-<hash>`, main HEAD reset     |
|         |                   | (`post.lua` beg block, `reset --hard HEAD~2`) |
| detect  | `heads blocked`   | `git rev-parse refs/begs/beg-<hash>` /      |
|         |                   | `chain list begs`                           |
| unblock | pioneer `like`    | pioneer `like 1 post <hash>` -> `like.lua`  |
|         | `<hash>`          | detects beg, merges branch into main,       |
|         |                   | transfers reps, deletes the ref             |

Harness decision logic (replaces old post + `heads blocked`):

```
reps = `chain '#c' reps author <PUB>`
if reps <= 0:  post --beg --sign=key ; pioneer like 1 post <hash>
else:          post --sign=key
```

Note `<= 0`, not `== 0`: reps can go negative after dislikes; the
gate in `apply` is `reps <= 0`. Item (d) "extra likes" accounting
stays harness-side (as in old `common.lua`) — no CLI query needed.

## Spike: confirm beg/unblock before porting

Single root, mocked time; pass = `blocked_ok`, `unblocked_ok`,
`ref_clean`, and reps shifted pioneer -> newbie.

```sh
ROOT=/tmp/spike; rm -rf $ROOT
ssh-keygen -t ed25519 -N '' -C '' -f /tmp/pio
ssh-keygen -t ed25519 -N '' -C '' -f /tmp/new
FC="freechains --root=$ROOT"
REPO="$ROOT/chains/#spike"

$FC --now=0   chains add '#spike' init inline --sign=/tmp/pio
H=$($FC --now=100 chain '#spike' post inline 'hi im new' --beg --sign=/tmp/new)

git -C "$REPO" rev-parse --verify refs/begs/beg-$H            # exists => blocked
$FC chain '#spike' list order | grep -q $H && echo MAIN_BAD || echo blocked_ok

$FC --now=200 chain '#spike' like 1 post $H --sign=/tmp/pio   # welcome

$FC chain '#spike' list order | grep -q $H && echo unblocked_ok
git -C "$REPO" rev-parse --verify refs/begs/beg-$H 2>/dev/null && echo REF_LEAK || echo ref_clean
$FC --now=200 chain '#spike' reps author "$(cat /tmp/pio.pub)" # pioneer -1
$FC --now=200 chain '#spike' reps author "$(cat /tmp/new.pub)" # newbie +~1
```

Pass `--now=200` to the `reps` checks too: without it `CMD.now`
defaults to real time (~2026 >> 200), firing consolidation scans
(`time > G.now`) that inflate the newbie's reps. Direction holds
either way, but `--now` keeps the numbers clean.

## Original sim files (to port)

| file                      | role                                     |
|---------------------------|------------------------------------------|
| `chat/common.lua`         | harness: `post()` + auto-like + epilogue  |
| `chat/chat-02.lua`        | Wikimedia IRC replay (single host, 5k cap) |
| `chat/chat-03.lua`        | multi-host TCP gossip (N=5, syncs=3, 10k) |
| `chat/chat-03.sh`         | per-port start/stop/join/leave/sync + consensus/blocked measuring |
| `usenet/use-01.lua`       | mbox -> chronological preprocessing (already run) |
| `usenet/use-02.lua`       | comp.compilers replay (single host, cap DISABLED -> full archive) |
| `usenet/use-03.lua`       | multi-host TCP gossip (N=12, syncs=4, 10k) |

`use-03.lua` is the news-side fork-ratio driver (item c) and MUST be
ported too; `chat-03.sh` is the only converge/measure tool and needs
a vcs equivalent (`sims/peers.sh`).

## Datasets (already fetched, `old/*.zip`)

Fetch + preprocess is DONE; `use-01` is kept for provenance only.

| zip                       | expands to           | used by            |
|---------------------------|----------------------|--------------------|
| `wikimedia.chat.zip`      | `wikimedia.chat` 13M | chat-02, chat-03   |
| `comp.compilers.mbox.zip` | 117M raw mbox        | use-01 input       |
| `xxx.mbox.zip`            | 89M mbox, UNSORTED   | (intermediate)     |
| `yyy.mbox.zip`            | 89M mbox, sorted     | use-02, use-03     |

Usenet lineage (verified, not what the file names suggest):

```
comp.compilers.mbox  117458214 B  33987 recs  full RFC822 headers
    | use-01: keep From/Subject/Date + body, drop the rest
    v
xxx.mbox              88863562 B  33814 recs  reverse-chrono runs
    | use-01 again: re-parse + sort by ts
    v
yyy.mbox              88863545 B  33814 recs  ascending
    | paper.lua: first 10.000 records
    v
comp-10k.mbox         31533931 B  10000 recs
```

- `xxx` is HALF DESCENDING (17076 asc / 18067 desc adjacent pairs);
  replaying it breaks `time >= G.now - diff`.
  `use-02.lua` reads `xxx.mbox` -> the port MUST read `yyy.mbox`.
- the 17-byte `xxx`/`yyy` delta is exactly 2 records whose `From:`
  drifted across a record boundary (`read_until` scans forward);
  same mechanism drops 173 records from raw -> `xxx`.

## Paper claims (verified by `sims/*/paper.lua`)

| claim (sec.consensus.eval) | paper  | measured    |
|----------------------------|--------|-------------|
| chat: period               | 3 mo   | 83.4 d      |
| chat: archive size         | 800kB  | 800269 B    |
| chat: per message          | 80B    | 80.03 B     |
| chat: users (item d)       | 80     | 80          |
| news: period               | 9 yr   | 8.78 yr     |
| news: archive size         | 30MB   | 31.53 MB    |
| news: per message          | 3kB    | 3153 B      |
| news: users (item d)       | 5000   | 5257        |

All confirmed.
Note the chat's 80B/msg counts the log framing (timestamp + nick);
actual payload is 47.36 B/msg (59%), which matters when computing
item (a) overhead against a baseline.

## Multi-peer TCP gossip (chat-03 / use-03 shape)

- pioneer peer creates the chain; all other peers `clone` from it
- per message:
    1. map author -> a fixed peer port (sticky)
    2. post signed by that user with `--now=ts`
    3. random `NSYNCS` distinct peers: bidirectional `sync send`
    4. unblock begs (see divergence below)
- final convergence sweep: `send` up the port range then back down
- this reproduces forks/blocked metrics (paper items c, d)

Peer/sync counts come from the sources, NOT the paper text:

| driver     | NPEERS | NSYNCS | cap   |
|------------|--------|--------|-------|
| `chat-03`  | 5      | 3      | 10000 |
| `use-03`   | 12     | 4      | 10000 |

Two behavioral divergences to preserve (or deliberately unify):

- `chat-03` also unblocks on the REMOTE peer after each sync
  (`fc_blocked(p)` -> pioneer like); `use-03` does not.
  This shifts the blocked ratio (item d) between the two drivers,
  so they are not directly comparable unless made uniform.
- both broadcast `fc_now` to ALL peers before each post (old daemon
  held a global clock). In the vcs there is no daemon clock: `--now`
  is per-command, so this broadcast simply disappears from the port.

## Four evaluation items (paper targets)

| item | metric              | paper chat | paper news |
|------|---------------------|------------|------------|
| a    | metadata overhead   | 10x        | ~41%       |
| b    | consensus init/incr | 125s/50ms  | ~65s/70ms  |
| c    | fork ratio          | 18%        | 14%        |
| d    | blocked ratio       | 3.7%       | 3.5%       |

Paper (d) is ONE discounted ratio, not two percentages:

```
(d) = (nlikes - U) / (nmsgs - U)
```

Discount one initial message + one welcoming like per user, on
BOTH numerator and denominator (paper example: 10 users, 110
msgs, 20 likes -> (20-10)/(110-10) = 10%).
`U` = welcomed users (pioneer posts but never begs, so U = 78 not
79 for the chat-02 10k run).

WARNING: the code's `(c) new` / `(d) extra` lines are NOT the
paper's items. Both letters are reused for a split of welcome
likes (new-user vs re-beg), each divided by raw `ns` with no
discount. Neither printed line equals the paper ratio; compute
(d) from the formula above. The paper's real item (c) is the
FORK ratio (18% chat), a multi-host metric measured in step 7,
never in this single-host run.

## Steps

1. Run the beg/unblock spike above to empirically confirm
   (retires the last CLI unknown).
2. Deliverable location = this worktree, e.g.
   `/x/papers/26-06-vcs/sims/` (NOT `/x/x/freechains/vcs/`, which is
   outside the worktree and off-limits for edits).
3. Port `common.lua` harness:
    - `fc_start` -> daemon `--hub`; `fc_stop` -> `pkill`
    - keys -> per-user `ssh-keygen` + cache pubkey string
    - post/like -> `--now=ts ... --sign=<keyfile>`
    - 0-rep decision -> `post --beg` then pioneer `like 1 post <h>`
    - blocked -> `list begs`
    - send/recv -> `sync send/recv git://localhost:P`
    - consensus -> `list order`
4. DONE — archives fetched + preprocessed (`old/*.zip`); just unzip.
   `sims/chat/paper.lua` and `sims/usenet/paper.lua` verify the paper
   claims and emit the 10k slices (`wikimedia-10k.chat`,
   `comp-10k.mbox`), which are the replay inputs.
5. Port `chat-03.sh` -> `sims/peers.sh` (start/stop/clone/sync +
   `list order` / `list begs` measuring across a port range).
6. Port single-host replay (`chat-02`, `use-02`) — sizes,
   consensus time, blocked ratio (items a, b, d).
7. Port multi-host TCP drivers (`chat-03` 5/3, `use-03` 12/4) —
   fork ratio (item c).
8. Run, tabulate, document divergences vs paper.

## Risks / caveats

- Beg/unblock (old high risk) RESOLVED by source read: `post --beg`
  + `like 1 post <hash>` (pending empirical spike run).
- Reps constants changed since the paper -> absolute numbers
  diverge; report methodology match, not figure match.
- `git daemon` receive-pack must run hooks; confirm push path
  end-to-end over `git://` (tests only cover local-path send).
- Each peer = one `--root` dir + one daemon port (mirrors the
  original one-host-per-port model).
- Monotonic `--now`: only `yyy.mbox` and `wikimedia.chat` are
  time-sorted (post requires `time >= G.now - diff`).
- The `reps` gate MUST be queried with the same `--now=<ts>` as the
  post it gates; without it the query uses real time (~2026), fires
  consolidation scans, and reports an inflated balance -> the post
  then fails with "insufficient reputation" (hit in `chat-02`).
- A post costs ~1 rep with slow recovery, so authors fall back to
  `<= 0` and beg again; the extra likes ARE item (d).

## Pending decisions

- Sim scope (multi-peer chat only vs full chat+usenet suite).
- Transport fidelity: push-based `send` (exact) vs pull-only
  `recv` (simpler, same end state).
- Peer/sync counts: keep source values (chat 5/3, news 12/4)?
- Message cap, per driver:
    - `chat-02` 5k, `chat-03` 10k, `use-03` 10k (source values)
    - `use-02` has NO cap (full 89M archive) -> must pick one
- Unblock policy: replicate `chat-03`'s remote-peer unblock, or
  make chat and usenet uniform (affects item d)?
- Deliverable repo: RESOLVED -> this worktree
  `/x/papers/26-06-vcs/` (vcs is off-limits for edits).

## Handoff (2026-07-23) — chat-02 single-host, cross-machine

State of `sims/chat/chat-02.lua`: runs end-to-end as a flat
single-root port (no shared `common.lua` yet).
Continuing elsewhere needs the invariants and the open blocker below.

### Working invariants (do NOT regress)

| invariant | why |
|-----------|-----|
| `BASE` is ABSOLUTE (`realpath -m ../.freechains`)     | `git -C <repo>` chdirs first, so a relative `--sign` keyfile fails "invalid sign key" |
| root + keys split: `BASE/root` (`--root`), `BASE/keys` (ssh) | freechains owns `<root>/keys`; keeping ours separate avoids a keystore collision |
| shared root at `sims/.freechains`  | reused by chat AND usenet; `sims/.gitignore` ignores it; each run `rm -rf BASE` |
| post form `post --sign=K [--beg] inline -- '<msg>'` | `--` ends option parsing so a message starting with `-` (e.g. line 2867 `-d runs on 1.16wmf4`) is text, not a flag; `--sign`/`--beg` sit on the parent `post`, so `--` can't swallow them |
| reps query uses the SAME `--now=ts` as the post | else real-time (~2026) fires consolidation, inflates balance |
| `(c)/(d)` split: `nnew` (first welcome / new user) vs `nextra` (re-beg of an existing user), each `/ns` | harness-side accounting only; NOT the paper's items (see below) |

NOTE (2026-07-24): the original `(nlikes-#T)/(ns-#T)` formula was
NOT wrong — the paper's item (d) IS exactly that discounted ratio
`(nlikes - U)/(nmsgs - U)`. The only fix is `#T` = welcomed users
(78), not all users (79): the pioneer posts but never begs, so he
has no first welcome. That ±1 is the whole "off-by-one", not a
flaw in the formula's shape. The `nnew`/`nextra` split is fine as
a breakdown but must be folded back to the discounted ratio to
compare with the paper.

### BLOCKER — pioneer solvency (decide first on the new machine)

- Welcome likes come from Ashlee ALONE (`chat-02.lua`).
  Each `like 1` costs the liker 1 ext rep permanently (10% burned,
  half to the post); the liker is capped at `reps.max`.
- Full 10k needs ~1375 welcome likes (paper's old figure) >> cap.
  Single Ashlee ran dry at N~121 with the stock cap of 30.
- STOP-GAP IN USE: `reps.max` raised 30 -> 300 by editing
  `/usr/local/share/lua/5.4/freechains/constants.lua`.
  This is a SYSTEM file OUTSIDE this worktree — it does NOT travel
  with the repo. On the new machine either:
    1. re-apply the same edit (find via `which freechains` ->
       wrapper -> rock dir; sibling `share/.../constants.lua`), or
    2. adopt Option A (community-funded welcome: an established
       `reps>0` member likes, not Ashlee) — was prototyped then
       reverted in favor of the cap bump; git has no record, so it
       must be re-implemented if chosen, or
    3. seed N pioneers (paper lists 6) and rotate the welcome.
- 300 may STILL be < 1375; Ashlee's own ~871 posts refund reps
  over 12h/24h maturation, which MAY keep him solvent — unverified.
  First task on the new machine: run to 10k and watch for
  "insufficient reputation".

### Environment the new machine must match

- `freechains` v0.20.0 (rock `dev-2`) on PATH; `lua5.4`; `ssh-keygen`.
- Input `sims/chat/wikimedia.chat` (13M, from `old/*.zip`) present.
- Run from `sims/chat/`: `lua5.4 chat-02.lua`.

## Explicit next steps (ordered)

1. On the new machine, reconcile the `reps.max` stop-gap (see
   BLOCKER): re-apply the constants edit OR switch to Option A /
   multi-pioneer. Record the choice here.
2. Run `chat-02.lua` to the full 10k cap. Capture the summary
   (`users | likes | msgs`, `(c) new`, `(d) extra`). If it dies with
   "insufficient reputation", escalate per step 1.
3. Sanity-check items (a)/(b)/(d) from this single-host run:
    - (a) chain size vs archive: `du -sh sims/.freechains/root/chains/'#chat'`
      vs 800kB payload (10x target).
    - (b) consensus time: `time freechains --root=... chain '#chat' list order`.
    - (d) blocked ratio = `nnew+nextra` over msgs (already printed).
4. Decide the parser hardening scope: `--` fixes leading-dash, but
   confirm no other argparse escapes remain over the full 10k
   (messages are single-quoted; `'` is stripped at read).
5. THEN resume the main port backlog (unchanged): `sims/peers.sh`
   (Step 5), `use-02` single-host (Step 6), multi-host `chat-03`
   5/3 + `use-03` 12/4 (Step 7), tabulate (Step 8).

## Progress

- [x] 1 beg/unblock spike (all 4 assertions green, v0.20.0)
- [x] 2 deliverable at `sims/`
- [x] 4 datasets + `paper.lua` claim checks (chat + usenet)
- [ ] 3 harness (`chat-02.lua` is a flat standalone port, no
      shared `common.lua` yet -- keep it flat for now)
- [ ] 5 `sims/peers.sh` (port of `chat-03.sh`)
- [~] 6 single-host replays: `chat-02` DONE @10k (2026-07-24):
      79 users, 9580 msgs, 420 likes, ran clean, NO insufficient-
      reputation crash -> pioneer solvency HELD (reps.max=300 +
      post refunds). Item (d) = (420-78)/(9580-78) = 3.60% vs
      paper 3.7% (match). BLOCKER retired. `use-02` still TODO.
- [ ] 7 multi-host: `chat-03` (5/3), `use-03` (12/4)
- [ ] 8 tabulate vs paper
