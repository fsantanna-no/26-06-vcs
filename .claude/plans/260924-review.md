# Paper Review: main.tex vs vcs code (2026-09-24)

# Context

- full read of `main.tex` (active text, ln 1-1175; `\iffalse` after)
- scratch build: 11 pages, no overfull boxes, no undefined refs/cites
- code/docs: `/x/x/freechains/vcs` (HEAD 4c4ff8b, 2026-09-20)
    - `src/freechains/constants.lua`, `chain/{rules,consensus,sync}.lua`
    - `doc/{reps,cli,guide}.md`, `.claude/plans/threats.md`
- severity: critical / recommended / light
- workflow: one item at a time; cur (verbatim) -> new; end with "done:"
- no `% TODO` markers in tex/bib

# Issues (most to least severe)

1. [critical] [abstract ln 88; intro ln 190; §4-6 ln 1167-1174]
   TODO placeholders and empty sections
    - cur: "(TODO: evaluation results)", "(TODO: sync time, disk size)"
    - new: postponed; tracked in 260920-eval.md and TODO.md
    - won't do here

2. [light] [§3.6 ln 816-818, 869-870] exclusive members vs code
    - cur: "we first determine the exclusive members of each side";
      recap "Split members into disjoint sets at the fork point"
    - code: nobody excluded; a key on both sides counts on both sides
      (`consensus.lua:88-136`)
    - equivalent: same r added to both sums, winner and ties unchanged
    - was rated critical by mistake (mismatch not checked for effect)
    - done: won't do (user, 2026-09-24)

3. [light] [§3.8.1 ln 1002] citation for Byzantine agreement
    - cur: "classic Byzantine agreement with permissioned
      quorums~\cite{p2p.byz}" (Kleppmann, BFT CRDTs)
    - rechecked: p2p.byz backs "permissioned quorums" (P2P, Sybil);
      Lamport 1982 backs the classic >2/3 bound and majority vote
    - was rated critical by mistake (judged by title, not text)
    - done: `\cite{lamport.byz,p2p.byz}`; bib lamport.byz -> 1982
      TOPLAS article (fixes empty publisher, part of item 17);
      pdfs/lamport.byz-lamport1982.{pdf,txt} added

4. [recommended] [intro ln 178-179; §2 ln 242-243; tab ln 223]
   "relying only on hooks" overstates
    - code: Lua CLI façade over git; hooks only pre-receive on hubs
      (`sync.lua`, `daemon`)
    - cur: "relying only on hooks to add reputation, moderation, and
      consensus"; Table 1 `reps` -> Git "hooks"
    - new: "relying on a thin command façade and hooks"; Table 1 reps
      -> "replay" (or "log + hooks")
    - done: dropped "only" at ln 178, 242 (user); Table 1 row kept

5. [recommended] [Table 2 ln 412-413] rule 4.c not enforced
    - code: `constants.lua:28-30` `post.size` commented out; vcs plan
      260903-128KB: "transport policy, not a consensus rule"
    - cur: "Payloads are limited to at most 128 KB."
    - new: footnote "enforced at transport, not by consensus" or enable
      in code

6. [recommended] [§3.8.2 Listing 10 ln 1022-1030] unverified scenario
    - discard across a sync merge: `discard` drops a linear suffix;
      260819-blacklist notes received history resurrects on next recv
      (fine here: dislike now precedes it)
    - `discard 7c1e2a9` unexplained (the sync merge tip)
    - `dislike 5000 member c` lacks `--sign=a` unlike other listings
    - new: run scenario in vcs (`tst/`); add `--sign=a`; comment on
      7c1e2a9 "# sync merge"

7. [recommended] [§3.6 ln 817-818, 871-872] reps "at the fork point"
    - code: reps at octopus merge-base floor (`sync.lua:158-162`),
      negative balances included; equals fork point only for a
      simple two-way fork
    - new: "held at the common ancestor of all open branches"; keep
      example unchanged

8. [recommended] [§3.9 ln 1150-1152; §2 ln 249-250] convergence
   depends on sync timing
    - hard-fork rule makes admission time-dependent (threats.md T1a):
      two honest peers syncing the same remote before/after settling
      end with different DAGs, permanently
    - cur: "Strong convergence holds by construction ... over the DAG"
    - new: add "for peers holding the same DAG; the settling window
      may split honest peers by sync timing (§3.8.3)"

9. [recommended] [§3.7 ln 910; Fig 2 label] lower bound wording
    - code: "too old" vs newest timestamp in causal ancestry
      (`rules.lua:437-444, 492-502`), not the parent's own time
    - cur: "no older than its newest parent"
    - new: "no older than the newest timestamp in its ancestry";
      Fig 2 `parents-1h` -> `ancestors-1h` (times.py)

10. [light] [§3.8.1 ln 996] blacklisting not implemented
    - vcs plan 260819-blacklist open
    - cur: "drop the connection and blacklist the remote peer"
    - new: "reject the sync and possibly blacklist the remote peer"

11. [light] [§3.6 ln 873; §3.5 ln 798] tie breaker precision
    - code: lexicographically smaller cid wins; York 3d5f < Xion 7468
    - cur: "(tip hash as tie breaker)"
    - new: "(lowest tip hash as tie breaker)"

12. [light] [§3.6, §3.8] dictators unmentioned
    - code: `--dictator` ungated, side with dictator always wins
      (`consensus.lua:163-171`)
    - deferred to evaluation (done/260905-review 6.8)
    - won't do now

13. [light] [§2 listings; Table 1] CLI syntax simplified
    - code: `fc chain /chat get payload`, `sync recv`, metadata as
      Lua table
    - new: footnote at Table 1 "listings abbreviate the CLI"

14. [light] [§3.7 ln 918; Fig 2] label case
    - cur: text `OK`; figure `ok`
    - new: text `ok`

15. [light] [§3.4 ln 728-729] unrevoke semantics
    - code: revoked iff author's or others' revoke balance < 0;
      likes also count as unrevokes (`rules.lua:21-24, 622-635`)
    - cur: "according to the balance between votes"
    - new: "according to the balance between revokes and likes"

16. [light] [§3.8.1 ln 998-1000] DoS form
    - threats.md T6a: disk exhaustion via fetch (no size limit)
    - cur: "it can only deny service"
    - new: "it can only deny service or exhaust resources"

17. [light] [bib ln 148, 82-88, 155-158] bibtex nits
    - pos.weak: "How I" -> "How {I}" (renders "How i")
    - hashcash, p2p.bitcoin: empty institution; lamport.byz: empty
      publisher (add fields or switch to @misc)

18. [light] [bib] unused p2p.nostr, p2p.sec
    - related work postponed
    - won't do

19. [light] [§3.3 ln 634-635] refund formula
    - code: 12h * max(0, 1 - 2*ratio); immediate past 50% of reps
    - cur: "or sooner in proportion to the reps of members acting
      after it"
    - won't do (close enough)

# Verified OK (no action)

- Listings 5-9 arithmetic (tax, split, cost, refund, reward, tie)
- rules 1.a, 1.b, 2, 3.a-3.d, 4.a, 4.b match `constants.lua`/`rules.lua`
- settling: `time.apply` = position timestamp; idle/winner caveats
- Fig 1 numbers (5+3>6, R1=6, R2=8); Fig 3 (S4=4 -> P4=12)
- all cite keys resolve; layout clean

# Pending

- next up: item 2 (decide text vs code)
