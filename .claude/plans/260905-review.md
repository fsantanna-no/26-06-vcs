# Paper Review: main.tex (2026-09-05)

# Context

- full read of `main.pdf` / `main.tex` (active text, ~lines 1-1080)
- disabled block (`\iffalse`, after Hard Forks) holds old sections
- four passes: (1) nits, (2) concepts, (3) new ideas, (4) shrink
- workflow (see memory `feedback-review-format`):
    - one item at a time: 2.x problem, then 2.x.y verbatim + fix
    - severity tags: critical / recommended / light
    - no `% TODO` markers in tex/bib (user, 2026-09-21)
    - user reviews and rewrites; check their version, no compile
- last state (2026-09-21): passes 5 (text), 6 (code), 7 (design)
  recorded; 3 and 4 open; sections postponed
- pass 6 is being applied one item at a time; 6.1-6.2 closed
- today's commits (9e496c1..d8655f5): intro rewrite (4 contribs,
  minor contribs and Sybil limitation dropped, Wiki/CRDT outline
  commented), SZT comments removed, Git paragraphs merged
- coauthor comments in tex as `% SZT.n:`; keep them, renumber if moved

# Pending (all open items, in order)

- next up: pass 4 (shrink), first batch 4.3, 4.10
- postponed by user (2.16): evaluation, related work, conclusion
    - 6.8 deferred here: define dictators in the evaluation, since
      the gated wiki/github runs use them
    - pass 1 deferred here: TODO marker ln 200 (results); refs
      sec.evaluation, sec.related, sec.conclusion
    - 3.9 deferred here: replay a real attack (wiki farming +
      refutation)
    - 7.3 deferred here: measure actions voided per failure in the
      sims (rare or common amplification)
- outside this repo, tracked in vcs:
    - .claude/plans/260921-discard.md, discard across a sync merge
    - enable rule 4.c (128 KB) in constants.lua
    - guide.sh still uses FORK+7*DAY while guide.md moved to day 8
- restore memory `feedback-review-format` here, copy from the other
  machine (missing on this one; the workflow above is its summary)

# Pass 1: nits, grammar, small changes

- [x] grammar, hyphenation, pronouns, agreement (bb15166)
- [x] Table 3 verbs (grants/charges), `128~KB`
- [x] table labels `tab.general`, `tab.rules`
- [x] bib caps protection (Sybil, IPFS, Bitcoin, CRDTs, Nostr)
- [x] "100 actions" unified; tax target "(member or post)" (89d4812)
- [x] 1.a: intro tone "control moderation and monetize user data"
- [x] 1.b: intro contributions as one paragraph, one sentence per
      item, italic term inline (order: PoA, moderation, forums,
      consensus); limitations: long-range attack + performance
- [x] Figure 2 redrawn by user as imgs/hard.dia (labels "100 actions")
- [x] TODO markers: ln 200 (results) deferred to the evaluation
- [x] undefined refs: sec.evaluation, sec.related, sec.conclusion
      deferred (postponed sections); sec.crdts only in a comment;
      sec.design.revoke no longer referenced
- [x] Listing 2 overflow (4.25pt): payload hash truncated (ln 311)
- [x] Table 1 overflow (27pt, sync row): tabcolsep 3pt,
      "Erases/restores payload", sync "Exchanges actions" and
      \code{fetch/push/merge}, @{}lll@{}; 14.4pt then 2.5pt left
      after each try; compile to confirm

# Pass 2: conceptual issues

## Done

- [x] 2.1 PoS analogy (critical)
    - 2.1.a intro limitation: long-range attacks, pos.weak
    - 2.1.b 3.5: reputation as stake, holding vs authoring, cap
    - 2.1.c 3.5: total reps at fork point, regardless of origin
    - 2.1.d 3.5: both-sides members excluded; nothing-at-stake
    - 2.1.e footnote: tie-breaker subject to stake grinding
    - 2.1.f 3.6.2: names long-range attack
    - 2.1.g 3.6.2: settling window = checkpoints, weak subjectivity
    - 2.1.h bib: pos.ouroboros, pos.weak
    - 2.1.i 3.6: outcome deterministic from common history
    - 2.1.c/d reverted by user (nothing-at-stake does not apply)
    - pending: related work cites (Ouroboros, Casper, Buterin)
- [x] 2.2 zero-sum vs per-identity emission (critical)
    - 2.2.a intro: Sybil resistance not absolute, fake identities
      must replicate human behavior
    - 2.2.b 3.2: zero-sum only for cycling reps among Sybils
    - 2.2.c 3.3: limitation paragraph, splitting identities,
      threat is automated content
- [x] 2.3 refuse merge vs local first (critical)
    - 2.3.a 3.6.2 summary: merge refused if j wins and reorders
      settled prefix (confirmed in vcs/src/freechains/chain/sync.lua)
- [x] 2.4 DAG untouched vs erased (recommended)
    - 2.4.a: order decides which incoming posts are merged
    - 2.4.b: rejected actions never merged into local DAG
- [x] 2.5 Bitcoin revocation (light)
    - 2.5.a: "lack of support for revoking payloads at protocol level"
- [x] 2.17 content rating missing (recommended)
    - 2.17.a: new 3.4 Content Rating + roadmap entry
    - 2.17.b: intro para split; reputation systems sentence
      (rep.systems); Slashdot/Reddit/StackOverflow cites
    - bib: forum.slashdot, forum.reddit, forum.stackoverflow,
      rep.systems
    - 2.17.c: post reps as balance of likes/dislikes
    - 2.17.d: Listing lst.rating (like+dislike cancel), text
      (values from real run, scratchpad/rating)
    - 2.17.e: Table 3 new rule 3.b split (action + author);
      like/dislike -> 3.c, revoke/unrevoke -> 3.d; refs fixed

- [x] 2.6 "consensus" oversold (recommended)
    - 2.6.a intro contribution renamed "Local-first consensus";
      offline members, both sides legitimate, cite cap
    - 2.6.b 3.7: contrast with PoW/PoS online majority; availability
      over consistency; hard fork instead of reorg
    - 2.6.c abstract: "consensus on the order of posts"

## Next steps (in order)

1. Pass 3 (new ideas), then Pass 4 (shrink)
2. Postponed (2.16): evaluation, related work, conclusion; the intro
   roadmap still promises them (three "Section ??")
3. TODO.md item: regenerate Table 3 into vcs/doc/reps.md

## Done: 2.8.c Chain Time subsection (3.7), 2026-09-16

- [x] text: durations; chain time principle; untrusted timestamps;
      bounds as one sentence; admission local, decisions
      chain-determined (btc.timestamps); position timestamps
      P_i = max(P_{i-1}, S_i); monotonic timeline
- [x] figs: imgs/times.* (tolerance window), imgs/monotonic.* (DAG vs
      monotonic order); imgs/hard.* restored and relabeled
      (settled/unsettled, lose/win/ok); all pngs at 1600px
- [x] 3.8.2 rewritten: single version, losers merged, winners into
      settled prefix refused; matches vcs impl (time.apply)
- [x] 1.c: Sybil limitation cites p2p.merkle-crdts, p2p.byz
- [x] bib rewritten compactly (2026-09-19): 52 entries, 2-3 lines
      each (first author + others, title, venue, year); dropped
      p2p.sec, p2p.prune, time.lamport (uncited) and the duplicate
      p2p.rep.eigentrust (disabled cite repointed to rep.eigentrust);
      disabled-only entries kept under a marker comment; p2p.nostr
      kept for related work; all lines <= 80 cols
- [x] dropped by user in 3.8.2: idle-forum limitation (2.13.h),
      "discard and repost", "7-day window as chain constant"; revisit
      in pass 3/4 if wanted
    - idle forum restored as a one-line caveat by 7.4.a

## Remaining (numbering as shown to user)

- [x] 2.7 (critical): "consolidated" and "tolerate" defined
    - impl was unconditional; rule changed: emission while not revoked
    - plan: 260907-revoke.md (paper) + vcs/.claude/plans/260907-revoke.md
    - 2.7.a Table 3 1.b "while not revoked (3.d)"
    - 2.7.b 3.3 tolerate = do not revoke
    - 2.7.c 3.3 T2 refund by reps activity; T3 unless revoked
    - 2.7.d 3.3 limitation: detect and revoke, cancels emission
- [x] 2.8 (critical): time-based rules vs unverifiable timestamps
    - impl: timestamp in [parents-1h, receiver now+1h]; chain time =
      newest accepted; rules advance in chain time
    - 2.8.a 3.3: chain time paragraph
    - 2.8.b 2: timestamps constrained for time-based rules
    - old disabled time section can stay disabled
- [x] 2.9 + 2.13 (critical): freeze-by-flood (redone 2026-09-09)
    - design: keep current semantics (accept losers, refuse a winner
      that reorders the settled prefix, 7 days), settle age by
      CONSENSUS TIME at replay, no action count
    - rejected: reject-old-forks win-or-lose (cheap boundary noise),
      local-arrival freeze (receipt gap), count (floodable)
    - vcs plan: vcs/.claude/plans/260909-forkage.md (not implemented)
    - 2.13.a intro: winners reordering settled actions rejected
    - 2.13.b roadmap: back to "settling window"
    - 2.13.c 3.7.1: "weeks" restored
    - 2.13.d 3.7.2: frozen 7 days after entering the order, losers
      loose and refutable, winner refused, window a chain constant
    - 2.13.e summary: three cases (loose vs frozen)
    - 2.13.f imgs/hard.dia: "frozen/loose actions", "7 days"; png
      1600px; caption "Settled local branch with frozen actions"
    - 2.13.h 3.7.2: idle-forum limitation (fast-forward, revoke only)
    - open: window as genesis constant (vcs)
- [x] 2.10 (recommended): merge semantics
    - impl: post has one parent (HEAD); sync creates a merge commit
      after the loser replay; like on a beg has two parents
    - 2.10.a sec 2: sync joins the tips with a merge commit
    - 2.10.b Table 1: sync row "fetch, push, merge"
- [x] 2.11 (recommended): general fork case
    - impl: sync compares two tips at their pairwise merge base;
      incoming DAG replayed on its own first (nested forks bottom-up);
      3+ peers = successive pairwise merges
    - 2.11.a 3.6: one sentence ("nested and multi-way forks reducing
      to successive pairwise merges"); Kahn algorithm stays disabled
- [x] 2.12 (light): double-spend example: no change
    - 3.5 only exposes the problem; the rule (and tie-breaker) is 3.6
    - Carol at 0 is not permanent: voided like erased, she keeps the
      surviving one; earns again via posts/likes; no text change
- [x] 2.14 (recommended): pay-to-revoke
    - covered by 3.4 (cost 1K, drains target/author, cancels reward,
      abuse vs disagreement, likes count toward unrevoke)
    - 2.14.a dropped by user: abusive revokes are already checkable
      via unrevoke/likes (3.4) and forks (3.8.2); no extra sentence
    - rule 4.c (128 KB) in Table 3 but commented out in
      vcs/constants.lua: paper keeps it; vcs TODO to enable
- [ ] 2.16 (critical): missing sections
    - [x] 2.16.a revocation in 3.4 (listing lst.revoke placeholder
      hashes; text: erase payload, prove by hash, unrevoke/likes
      restore, self-revoke free and final, cost 1K, cancels reward)
    - [x] revoke listing run for real (scratchpad/revoke): hash
      65980b7, error "revoked payload"; vote ids omitted
    - 4.a is a floor, not a uniform price; revoke at 1K is consistent
      (text says "twice the minimum"); no table change
    - [x] time: 3.7 Chain Time
    - consensus algorithm: keep disabled (user), superseded by 3.6
    - evaluation: POSTPONED (user); rewrite from
      /x/papers/2606-sims/*/RESULTS.md (sims split out 2026-09-20,
      github fsantanna-no/26-06-sims)
      when resumed; fold the wiki replay in as a subsection
    - CRDTs and Wiki: dropped from the paper; already commented out
      (abstract, keywords, roadmap) and the section stays inside the
      disabled block; no edit needed (my deletion reverted by user)
    - related work: POSTPONED (user); queued items in TODO.md
    - [x] 2.16.c Byzantine peers: restored as 3.9, trimmed (dropped
      the sync paragraph, duplicated in sec 2/3.6; dropped the peer
      blacklist claim, not implemented: vcs plan 260819-blacklist is
      still in discuss and bans branches, not peers); added the
      no-quorum point (p2p.byz) and the roadmap entry
    - blacklist WON'T DO (user): ln 1078 "blacklist the remote peer"
      stands as a local policy (stop syncing with it), outside the
      protocol
    - old subsections (old revocation, Public Forum Chains): keep
      disabled (user)
        - proof-of-stake fork choice: Ouroboros, Casper
        - weak subjectivity: Buterin 2014 (pos.weak)
        - web-of-trust Sybil defenses (SybilGuard/SybilLimit)
        - Slashdot mod points (forum.slashdot) as precedent
        - Matrix, Scuttlebutt, Nostr, ActivityPub
    - conclusion

# Pass 5: full review (2026-09-21)

- read of the whole active text plus rendered pages 4-10
- excludes the two missing sections and the known TODO markers

## Critical

- [x] 5.1 3.5 example contradicts its motivation: the text says a
      member with reps for a single POST posts SPAM from several
      peers, but Listings 10-11 double-spend 4500 on two LIKES
      (Xion, York); fix the sentence or the listing
- [x] 5.2 revoke minimum of 1K is prose-only: 3.4 says "at least
      1000, twice the minimum (4.a)", but Table 3 has 4.a=500 and
      3.d with no floor, while its caption promises all rules

## Recommended

- [x] 5.3 SUPERSEDED: the user already kept "(vs longest chain)"
      as a role pairing (see "Won't do")
- [x] 5.4 p2p.sec sits in the active bib group but is cited nowhere
      (3.9 cites p2p.crdts alone): cite it or move it to disabled
    - 5.4.a applied (user choice): main.bib, p2p.sec moved below
      the disabled marker; kept for related work
- [x] 5.5 WITHDRAWN: Figure 4 is correct. Settling is `>= 7 days`,
      so Pi at exactly 7 days behind the tip is settled and Pj is
      not; the arrow measures how far back the last settled action
      is. Optional cosmetic: move the left tick onto the divider so
      it points at the boundary instead of at a node.
- [x] 5.6 WON'T DO (user): reference style mixed: 8
      "Section~\ref" vs 26 "\S\ref";
      ln 681 and 798 are ordinary cross-refs
    - why: consistent already, "Section" in running text (roadmap,
      ln 682, 798) and \S in parentheses and lists (26)
- [x] 5.7 "In summary" opens five paragraphs (693, 958, 1010, 1087,
      1184), four inside Section 3; two restate the paragraph above
      (overlaps 4.2)
    - 5.7.a applied: ln 694 "Overall,"; ln 1007 "Thus,"; ln 1084
      phrase dropped (a contrast, not a summary)
    - kept: ln 1182 (introduces the three-case list); ln 955 is
      left to pass 4.2

## Nits

- [x] 5.8 "e.g." without comma at ln 129, "e.g.," at ln 98
- [x] 5.9 Listing 13 error: now "chain payload : revoked"
- [x] 5.10 "four main contributions (in italic)" describes typography
- [x] 5.11 panel style now "Figure 1(A)", matching the A./B.
      labels drawn in dag.dia and monotonic.dia
- [x] 5.12 dropped the four "(ln~N)" that pointed at "# PEER"
      comment lines; the following ln ranges already locate them

# Pass 6: code cross-check (2026-09-21)

- paper claims verified against /x/x/freechains/vcs

## Critical

- [x] 6.1 WITHDRAWN: "at least one honest member" is correct.
      The dislike EXTENDS branch-2, and collect_keys() gathers every
      signer from the merge base to the tip, so the refuting branch
      carries {a,b}=8 against c=6. One member running discard +
      dislike suffices, because the branch already holds both
      signatures.
- [x] 6.2 WITHDRAWN: verified by experiment with the real CLI.
      A (dislikes before merging) and B (had merged branch-1 first)
      both end at: like a1 b1 dis c1 c2. The peer that merged
      reorders branch-1 and converges exactly; only voided actions
      need reposting by their authors.
- [x] 6.2b CLOSED for the paper: Listing 12 and its paragraph are
      correct; `discard` just cannot cross a sync merge yet
    - verified: rewinding HEAD to the merge's first parent voids
      branch-1 and drops c1/c2 from `list order`, c at -3600
    - implementation only, tracked in the vcs repo:
      .claude/plans/260921-discard.md
- [x] 6.3 begs break Table 3 rule 2: Listing 6 shows Dave at 1800
      (no 500 charged); rules.lua bumps 0 for begs; also unstated:
      beg refused if member holds >=500, and refused on open chains
    - 6.3.a applied (user wording): main.tex ln 471-472, rule 2 reads
      "New post charges 500 to member temporarily (except begs),
      refunded within 12h."
    - 6.3.b dropped by user: the gated-chain and under-500 conditions
      are implementation details

## Recommended

- [x] 6.4 tie-break footnote says "action hashes after the fork
      point"; consensus.lua compares the two branch TIPS (merge
      parents) in winner(): `elseif a < b`
    - 6.4.a applied (user wording): main.tex ln 930, footnote
      dropped; inline "(tip hash as tie breaker)"
- [x] 6.5 3.4 self-revoke: a like lands on revoke.others and cannot
      clear revoke.member, so community likes never restore a
      self-revoked payload; add a clause
    - 6.5.a applied (user wording): main.tex ln 775-776, "balance
      between revokes and unrevokes (and likes)"
    - kept as is: the self-revoke sentence right after ("no other
      member can undo") states the exception
- [x] 6.6 WON'T DO (user): rules 1.a and 4.b are the SAME constant
      (chains.lua splits
      C.reps.max among pioneers); a single pioneer starts capped;
      Table 3 caption implies independent constants
- [x] 6.7 WON'T DO (user): open chains have no reputation
      criterion: unsigned actions share one `anonymous`
      account, both sides cancel, hash decides; Section 2's
      fork (Listing 4) is resolved that way; unstated
    - why: section 2 claims no order for its fork and no later
      section rates open chains; the anonymous tie is an
      implementation detail with no effect on any claim
- [ ] 6.8 DEFERRED to the evaluation rewrite: dictators are in
      the impl (a dictator side wins outright in winner()) and in
      the gated wiki/github sims, but absent from the paper;
      needed if Section 4 reports those runs
- [x] 6.9 WON'T DO (user): a single "too new" action aborts the
      WHOLE sync (replay trunc=false); 3.7 says "refuses syncs"
    - why: ln 1004 already matches (sync refused, converges on
      retry); far-future case is the Byzantine peer of 3.9

# Pass 7: design-level issues (2026-09-21)

## Severe

- [x] 7.1 DROPPED (user, minor): the per-identity cap turns
      reputation weight into identity count: at 50K (4.b) every
      long-lived member weighs the same, so fork choice
      reduces to counting distinct members in a branch;
      a 1000-post member equals a 50-post one; with 3.3's seeding
      incentive, holding many capped identities dominates holding one
      valuable one, and the long-run Sybil defense rests only on each
      identity producing tolerated content
      - the paper presents 4.b only as an incentive to spend
    - 7.1.a dropped by user: "and affect consensus" reverted; a
      minor effect that looks larger before consensus is explained
    - kept in 3.3: only "multiply emission"; no text in 3.6
    - design change (sublinear weight, higher cap): pass 3.1/3.8
- [x] 7.2 WON'T DO (user): selective delivery forces a hard
      fork on a chosen peer, cheaply and repeatably: send a branch
      to victim B only; it loses, is appended, settles after 7 days;
      any later honest
      branch forks before it, so hardfork() refuses and B is split
      until a human runs `discard`
      - profitable: rule 2 refunds the cost in 12h and 1.b pays 1K
        after 24h
      - general form: gossip delivery is asymmetric by nature and the
        settling rule makes any tail difference permanent after the
        window; 3.8.3 assumes convergence within it, but an adversary
        decides whether that holds
    - why: needs a 7-day eclipse, i.e. a malicious partition;
      3.8 already declares partitions unbounded and possibly
      malicious (ln 1046) and accepts the hard fork (ln 1211);
      requiring reconnection within 7 days would contradict it

## Limitations to state

- [x] 7.3 WON'T DO (text, user): one failed action voids every
      later action in its branch, by every member (causality requires it), but
      the amplification
      appears only as "legitimate actions must be reposted"; worth a
      sentence and a measurement in Section 4
    - why: ln 910-912 already say all remaining actions are voided,
      and the recap (ln 931-932) voids x and y with c
    - measurement deferred to the evaluation (see Pending)
- [x] 7.4 during idleness the tail never settles: position timestamps
      advance only with accepted actions, so whatever was inside the
      window when activity stopped stays re-orderable indefinitely
    - 7.4.a applied (user wording): main.tex ln 1180, "As a minor
      caveat, idle forums never settle their most recent actions."
    - supersedes the dropped idle-forum limitation (2.13.h)

# Pass 3: new ideas

- tags: [design] changes the protocol; [paper] text only;
  [eval] needs a simulation run
- [x] 3.1 WON'T DO (user): [design] drop the daily cap of 1.b; emission per
      post; rule 2 rate-limits, 4.b caps holdings; removes the identity
      splitting incentive (3.3 limitation)
    - why: protocol change; possible future work in the conclusion
- [x] 3.2 WON'T DO (user): [design] settling window as a genesis constant (3.8.2
      already says "set at creation"; vcs constant is global);
      chats ~1 day, newsgroups ~7 days; lets the evaluation vary it
    - why: user already dropped "window as chain constant" (3.8.2)
- [x] 3.3 WON'T DO (user): [design] unrevoke without a payload copy: keep a
      local encrypted copy until sweep so the revoker can undo alone (minor;
      maybe not worth it)
    - why: protocol change, minor; possible future work
- [x] 3.4 [paper] threat-model table: attack, bounding rule or
      mechanism, residual cost to honest members (Sybil admission,
      identity splitting, double-spend, farming, long-range,
      boundary noise, censorship); material already in sec 3
    - 3.4.a applied: main.tex end of 3.8 (before 3.9), Table
      tab.threats (threat, countermeasure, section) + sentence
    - costs ~0.3 column against pass 4
- [x] 3.5 WON'T DO (user): [paper] state the guarantees explicitly (3.6 or 3.8):
      (a) same DAG -> same order and reps; (b) connected honest
      peers converge; conflicting settled prefix -> hard fork by
      design; the SEC claim without a proof
    - why: 3.9 already states SEC's properties and where they break
- [x] 3.6 WON'T DO NOW (user): chain time as a contribution: intro list does not
      mention 3.7 (bounded timestamps, position timestamps,
      monotonic consensus timeline)
    - 3.6.a commented out by user: main.tex ln 182, "This order
      also yields a deterministic chain time, rather than trusted
      clocks."; kept in the tex to revisit later
- [x] 3.7 [paper] surface "proof-of-stake with authoring as stake"
      earlier (abstract still says "vs longest chain")
    - 3.7.a applied (user wording): main.tex ln 181-183, "reputation
      consensus, resembling proof-of-stake (pos.ouroboros)";
      limitation: "long-range attacks (pos.weak)", no PoS repeat;
      abstract kept
- [x] 3.8 WON'T DO (user): [design, future work] reputation-weighted settling
      window (rejected earlier as complex; note as future work)
    - why: protocol change; possible future work in the conclusion
- [x] 3.9 DEFERRED to the evaluation: [eval] replay a real attack: wiki dataset
      (53% revisions eventually revoked) with the farming + refutation scenario
      of 3.8.1, turning the threat discussion into measured behavior
    - see Pending, postponed evaluation
- [x] 3.10 WON'T DO (user): [paper] genesis constants table with defaults (50K,
      1K, 500, 10%, 12h, 24h, 7d, 128KB, 1h drift); names the knobs the
      evaluation can vary

# Pass 4: how to shrink paper

- current: 13 pages (sec 1-3 ~10.5 + refs 2.5); postponed sections
  will add 3-4; target: free 2-3 pages from what exists
- candidates by yield (column-lines, ~55 per column):
    - why: Table 3 already lists every constant, set at creation
- [x] 4.1 WON'T DO (user): 3.6 recap list (ln ~902-916): restates the paragraph
      above with the same numbers; keep the one-line result "common <- branch-2
      <- branch-1" inline (~14)
    - why: the recap is the mechanism at a glance; too important
      to be pure prose; it also holds the tie breaker (6.4)
- [x] 4.2 3.6 "Forks and merges raise..." + (i)-(iv) summary: reorg
      talk repeats 3.8, summary restates 3.6's opening; cut to two
      sentences (~18)
    - 4.2.a applied: main.tex ln 936-941, two sentences (reorg,
      repost, rare); dropped local-first note and (i)-(iv)
- [ ] 4.3 Listings 8+9 (double-spend, 20+9 lines): merge into one;
      drop repeated `reps members` blocks (~14)
- [ ] 4.4 3.5 prose: "legitimate in isolation" and "same replay
      order" are said again in 3.6; keep example + one bridge (~8)
- [ ] 4.5 Listing 5: drop ssh-keygen lines and second chain id;
      renumber ln refs (~4)
- [ ] 4.6 3.1: "concept of permissionless forums" duplicates intro
      Sybil paragraph; keep one (~4)
- [ ] 4.7 3.8 opening: blockchain contrast repeats 3.6 PoS analogy
      and intro contribution; one sentence (~6)
- [ ] 4.8 sec 2: Listing 2 (git log) + paragraph only show the
      genesis commit; fold into Listing 1's text (~10)
- [ ] 4.9 3.3: the two paragraphs after the T-list overlap T3;
      merge to one (~5)
- [ ] 4.10 Table 2 subsumed by Table 3's Operation column; drop (~12)
- [ ] 4.11 Figures 3 and 4 both show settled vs unsettled; drop
      Figure 4 if 3.8.2 cites Figure 3 panel B (~12)
- [ ] 4.12 intro Bitcoin critique (a)-(d): (b) and (c) repeat 3.6
      "holding vs authoring"; cut them (~5)
- total ~110 column-lines (~1.2 pages); the rest must come from
  short new sections or dropping one figure/listing pair (4.3, 4.11)
- suggested first batch (pure redundancy): 4.1, 4.2, 4.3, 4.10

# Side notes

- Aug 28 PDF (~/Downloads) matches sections 1-2 of f5a4e7e
- GitHub split diff base f5a4e7e...main, anchor sha256("main.tex")

# Won't do

- 2.15 Git SHA-1 note: impl hardcodes SHA-1 (state.lua tree ids), no
  SHA-256; Git's sha1dc refuses known collisions; user: won't do

- intro: Sybil-resistance caveat sentence (2.2.a) dropped by user
- intro: "a few honest members" vs 3.7.1 "at least one"; kept as is

- abstract "(vs longest chain)": role pairing, kept as is

- 6.6 rules 1.a and 4.b share one constant: user, too small a detail;
  one pioneer starts exactly at the cap, so behavior is unaffected

- 6.7 open chains order forks by tip hash (anonymous account): user;
  section 2 claims no order and gated chains carry the paper

- 6.9 too-new action aborts the whole sync: user; ln 1004 "refuses
  syncs" is accurate and the retry converges

- 7.2 eclipse forces a hard fork: user; a malicious partition,
  already unbounded in 3.8 and ending in the accepted hard fork

- 7.3 text on voided actions: user; ln 910-912 and the recap
  already show it; measurement deferred to the evaluation

- 5.6 reference style: user; "Section" in prose, \S in
  parentheses and lists, the usual convention

- 3.9 "blacklist the remote peer": user; a local policy, since
  every sync is started locally against a named remote

- 3.6 chain time in the intro: user, not now; sentence left
  commented at main.tex ln 182

- pass 3 design items (3.1, 3.2, 3.3, 3.8): protocol changes out of
  scope; 3.5 and 3.10 already covered by 3.9 and Table 3

- 4.1 consensus recap list: user; the mechanism at a glance, too
  important to be pure prose
