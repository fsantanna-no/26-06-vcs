# Paper Review: main.tex (2026-09-05)

# Context

- full read of `main.pdf` / `main.tex` (active text, ~lines 1-1080)
- disabled block (`\iffalse`, after Hard Forks) holds old sections
- four passes: (1) nits, (2) concepts, (3) new ideas, (4) shrink
- workflow (see memory `feedback-review-format`):
    - one item at a time: 2.x problem, then 2.x.y verbatim + fix
    - severity tags: critical / recommended / light
    - every edit gets `% TODO: <pass>.<item>.<letter>` in tex/bib
    - user reviews and rewrites; check their version, no compile
- last state (2026-09-19): pass 2 done except postponed sections
  (evaluation, related work, conclusion); next: pass 3
- today's commits (9e496c1..d8655f5): intro rewrite (4 contribs,
  minor contribs and Sybil limitation dropped, Wiki/CRDT outline
  commented), SZT comments removed, Git paragraphs merged
- coauthor comments in tex as `% SZT.n:`; keep them, renumber if moved

# Pass 1: nits, grammar, small changes

- [x] grammar, hyphenation, pronouns, agreement (bb15166)
- [x] Table 3 verbs (grants/charges), `128~KB`
- [x] table labels `tab.general`, `tab.rules`
- [x] bib caps protection (Sybil, IPFS, Bitcoin, CRDTs, Nostr)
- [x] "100 actions" unified; tax target "(member or post)" (89d4812)
- [x] 1.a: intro tone "control moderation and monetize user data"
- [x] 1.b: intro contributions as one paragraph, one sentence per
      item, italic term inline (order: PoA, moderation, forums,
      consensus); limitations: long-range attack + performance
- [x] Figure 2 redrawn by user as imgs/hard.dia (labels "100 actions")
- [ ] TODO markers (intro: results; SEC proof promise removed)
- [ ] undefined refs: sec.evaluation, sec.crdts, sec.related,
      sec.conclusion, sec.design.revoke
- [ ] Listing 2 line overflows frame

# Pass 2: conceptual issues

## Done

- [x] 2.1 PoS analogy (critical)
    - 2.1.a intro limitation: long-range attacks, pos.weak
    - 2.1.b 3.5: reputation as stake, holding vs authoring, cap
    - 2.1.c 3.5: total reps at fork point, regardless of origin
    - 2.1.d 3.5: both-sides members excluded; nothing-at-stake
    - 2.1.e footnote: tie-breaker subject to stake grinding
    - 2.1.f 3.6.2: names long-range attack
    - 2.1.g 3.6.2: settling window = checkpoints, weak subjectivity
    - 2.1.h bib: pos.ouroboros, pos.weak
    - 2.1.i 3.6: outcome deterministic from common history
    - 2.1.c/d reverted by user (nothing-at-stake does not apply)
    - pending: related work cites (Ouroboros, Casper, Buterin)
- [x] 2.2 zero-sum vs per-identity emission (critical)
    - 2.2.a intro: Sybil resistance not absolute, fake identities
      must replicate human behavior
    - 2.2.b 3.2: zero-sum only for cycling reps among Sybils
    - 2.2.c 3.3: limitation paragraph, splitting identities,
      threat is automated content
- [x] 2.3 refuse merge vs local first (critical)
    - 2.3.a 3.6.2 summary: merge refused if j wins and reorders
      settled prefix (confirmed in vcs/src/freechains/chain/sync.lua)
- [x] 2.4 DAG untouched vs erased (recommended)
    - 2.4.a: order decides which incoming posts are merged
    - 2.4.b: rejected actions never merged into local DAG
- [x] 2.5 Bitcoin revocation (light)
    - 2.5.a: "lack of support for revoking payloads at protocol level"
- [x] 2.17 content rating missing (recommended)
    - 2.17.a: new 3.4 Content Rating + roadmap entry
    - 2.17.b: intro para split; reputation systems sentence
      (rep.systems); Slashdot/Reddit/StackOverflow cites
    - bib: forum.slashdot, forum.reddit, forum.stackoverflow,
      rep.systems
    - 2.17.c: post reps as balance of likes/dislikes
    - 2.17.d: Listing lst.rating (like+dislike cancel), text
      (values from real run, scratchpad/rating)
    - 2.17.e: Table 3 new rule 3.b split (action + author);
      like/dislike -> 3.c, revoke/unrevoke -> 3.d; refs fixed

- [x] 2.6 "consensus" oversold (recommended)
    - 2.6.a intro contribution renamed "Local-first consensus";
      offline members, both sides legitimate, cite cap
    - 2.6.b 3.7: contrast with PoW/PoS online majority; availability
      over consistency; hard fork instead of reorg
    - 2.6.c abstract: "consensus on the order of posts"

## Next steps (in order)

1. Pass 3 (new ideas), then Pass 4 (shrink)
2. Postponed (2.16): evaluation, related work, conclusion; the intro
   roadmap still promises them (three "Section ??")
3. TODO.md item: regenerate Table 3 into vcs/doc/reps.md

## Done: 2.8.c Chain Time subsection (3.7), 2026-09-16

- [x] text: durations; chain time principle; untrusted timestamps;
      bounds as one sentence; admission local, decisions
      chain-determined (btc.timestamps); position timestamps
      P_i = max(P_{i-1}, S_i); monotonic timeline
- [x] figs: imgs/times.* (tolerance window), imgs/monotonic.* (DAG vs
      monotonic order); imgs/hard.* restored and relabeled
      (settled/unsettled, lose/win/ok); all pngs at 1600px
- [x] 3.8.2 rewritten: single version, losers merged, winners into
      settled prefix refused; matches vcs impl (time.apply)
- [x] 1.c: Sybil limitation cites p2p.merkle-crdts, p2p.byz
- [ ] bib: time.lamport unused, not printed by plain.bst (drop or
      keep for related work)
- [ ] dropped by user in 3.8.2: idle-forum limitation (2.13.h),
      "discard and repost", "7-day window as chain constant"; revisit
      in pass 3/4 if wanted

## Remaining (numbering as shown to user)

- [x] 2.7 (critical): "consolidated" and "tolerate" defined
    - impl was unconditional; rule changed: emission while not revoked
    - plan: 260907-revoke.md (paper) + vcs/.claude/plans/260907-revoke.md
    - 2.7.a Table 3 1.b "while not revoked (3.d)"
    - 2.7.b 3.3 tolerate = do not revoke
    - 2.7.c 3.3 T2 refund by reps activity; T3 unless revoked
    - 2.7.d 3.3 limitation: detect and revoke, cancels emission
- [x] 2.8 (critical): time-based rules vs unverifiable timestamps
    - impl: timestamp in [parents-1h, receiver now+1h]; chain time =
      newest accepted; rules advance in chain time
    - 2.8.a 3.3: chain time paragraph
    - 2.8.b 2: timestamps constrained for time-based rules
    - old disabled time section can stay disabled
- [x] 2.9 + 2.13 (critical): freeze-by-flood (redone 2026-09-09)
    - design: keep current semantics (accept losers, refuse a winner
      that reorders the settled prefix, 7 days), settle age by
      CONSENSUS TIME at replay, no action count
    - rejected: reject-old-forks win-or-lose (cheap boundary noise),
      local-arrival freeze (receipt gap), count (floodable)
    - vcs plan: vcs/.claude/plans/260909-forkage.md (not implemented)
    - 2.13.a intro: winners reordering settled actions rejected
    - 2.13.b roadmap: back to "settling window"
    - 2.13.c 3.7.1: "weeks" restored
    - 2.13.d 3.7.2: frozen 7 days after entering the order, losers
      loose and refutable, winner refused, window a chain constant
    - 2.13.e summary: three cases (loose vs frozen)
    - 2.13.f imgs/hard.dia: "frozen/loose actions", "7 days"; png
      1600px; caption "Settled local branch with frozen actions"
    - 2.13.h 3.7.2: idle-forum limitation (fast-forward, revoke only)
    - open: window as genesis constant (vcs)
- [x] 2.10 (recommended): merge semantics
    - impl: post has one parent (HEAD); sync creates a merge commit
      after the loser replay; like on a beg has two parents
    - 2.10.a sec 2: sync joins the tips with a merge commit
    - 2.10.b Table 1: sync row "fetch, push, merge"
- [x] 2.11 (recommended): general fork case
    - impl: sync compares two tips at their pairwise merge base;
      incoming DAG replayed on its own first (nested forks bottom-up);
      3+ peers = successive pairwise merges
    - 2.11.a 3.6: one sentence ("nested and multi-way forks reducing
      to successive pairwise merges"); Kahn algorithm stays disabled
- [x] 2.12 (light): double-spend example: no change
    - 3.5 only exposes the problem; the rule (and tie-breaker) is 3.6
    - Carol at 0 is not permanent: voided like erased, she keeps the
      surviving one; earns again via posts/likes; no text change
- [x] 2.14 (recommended): pay-to-revoke
    - covered by 3.4 (cost 1K, drains target/author, cancels reward,
      abuse vs disagreement, likes count toward unrevoke)
    - 2.14.a dropped by user: abusive revokes are already checkable
      via unrevoke/likes (3.4) and forks (3.8.2); no extra sentence
    - rule 4.c (128 KB) in Table 3 but commented out in
      vcs/constants.lua: paper keeps it; vcs TODO to enable
- [ ] 2.16 (critical): missing sections
    - [x] 2.16.a revocation in 3.4 (listing lst.revoke placeholder
      hashes; text: erase payload, prove by hash, unrevoke/likes
      restore, self-revoke free and final, cost 1K, cancels reward)
    - TODO: run the revoke listing for real hashes/error text
    - 4.a is a floor, not a uniform price; revoke at 1K is consistent
      (text says "twice the minimum"); no table change
    - [x] time: 3.7 Chain Time
    - consensus algorithm: keep disabled (user), superseded by 3.6
    - evaluation: POSTPONED (user); rewrite from sims/*/RESULTS.md
      when resumed; fold the wiki replay in as a subsection
    - CRDTs and Wiki: dropped from the paper; already commented out
      (abstract, keywords, roadmap) and the section stays inside the
      disabled block; no edit needed (my deletion reverted by user)
    - related work: POSTPONED (user); queued items in TODO.md
    - old subsections (Byzantine sync, old revocation, Public Forum
      Chains): keep disabled (user)
        - proof-of-stake fork choice: Ouroboros, Casper
        - weak subjectivity: Buterin 2014 (pos.weak)
        - web-of-trust Sybil defenses (SybilGuard/SybilLimit)
        - Slashdot mod points (forum.slashdot) as precedent
        - Matrix, Scuttlebutt, Nostr, ActivityPub
    - conclusion

# Pass 3: new ideas

- tags: [design] changes the protocol; [paper] text only;
  [eval] needs a simulation run
- [ ] 3.1 [design] drop the daily cap of 1.b; emission per post;
      rule 2 rate-limits, 4.b caps holdings; removes the identity
      splitting incentive (3.3 limitation)
- [ ] 3.2 [design] settling window as a genesis constant (3.8.2
      already says "set at creation"; vcs constant is global);
      chats ~1 day, newsgroups ~7 days; lets the evaluation vary it
- [ ] 3.3 [design] unrevoke without a payload copy: keep a local
      encrypted copy until sweep so the revoker can undo alone
      (minor; maybe not worth it)
- [ ] 3.4 [paper] threat-model table: attack, bounding rule or
      mechanism, residual cost to honest members (Sybil admission,
      identity splitting, double-spend, farming, long-range,
      boundary noise, censorship); material already in sec 3
- [ ] 3.5 [paper] state the guarantees explicitly (3.6 or 3.8):
      (a) same DAG -> same order and reps; (b) connected honest
      peers converge; conflicting settled prefix -> hard fork by
      design; the SEC claim without a proof
- [ ] 3.6 [paper] chain time as a contribution: intro list does not
      mention 3.7 (bounded timestamps, position timestamps,
      monotonic consensus timeline)
- [ ] 3.7 [paper] surface "proof-of-stake with authoring as stake"
      earlier (abstract still says "vs longest chain")
- [ ] 3.8 [design, future work] reputation-weighted settling window
      (rejected earlier as complex; note as future work)
- [ ] 3.9 [eval] replay a real attack: wiki dataset (53% revisions
      eventually revoked) with the farming + refutation scenario of
      3.8.1, turning the threat discussion into measured behavior
- [ ] 3.10 [paper] genesis constants table with defaults (50K, 1K,
      500, 10%, 12h, 24h, 7d, 128KB, 1h drift); names the knobs the
      evaluation can vary

# Pass 4: how to shrink paper

- current: 13 pages (sec 1-3 ~10.5 + refs 2.5); postponed sections
  will add 3-4; target: free 2-3 pages from what exists
- candidates by yield (column-lines, ~55 per column):
- [ ] 4.1 3.6 recap list (ln ~902-916): restates the paragraph
      above with the same numbers; keep the one-line result
      "common <- branch-2 <- branch-1" inline (~14)
- [ ] 4.2 3.6 "Forks and merges raise..." + (i)-(iv) summary: reorg
      talk repeats 3.8, summary restates 3.6's opening; cut to two
      sentences (~18)
- [ ] 4.3 Listings 8+9 (double-spend, 20+9 lines): merge into one;
      drop repeated `reps members` blocks (~14)
- [ ] 4.4 3.5 prose: "legitimate in isolation" and "same replay
      order" are said again in 3.6; keep example + one bridge (~8)
- [ ] 4.5 Listing 5: drop ssh-keygen lines and second chain id;
      renumber ln refs (~4)
- [ ] 4.6 3.1: "concept of permissionless forums" duplicates intro
      Sybil paragraph; keep one (~4)
- [ ] 4.7 3.8 opening: blockchain contrast repeats 3.6 PoS analogy
      and intro contribution; one sentence (~6)
- [ ] 4.8 sec 2: Listing 2 (git log) + paragraph only show the
      genesis commit; fold into Listing 1's text (~10)
- [ ] 4.9 3.3: the two paragraphs after the T-list overlap T3;
      merge to one (~5)
- [ ] 4.10 Table 2 subsumed by Table 3's Operation column; drop (~12)
- [ ] 4.11 Figures 3 and 4 both show settled vs unsettled; drop
      Figure 4 if 3.8.2 cites Figure 3 panel B (~12)
- [ ] 4.12 intro Bitcoin critique (a)-(d): (b) and (c) repeat 3.6
      "holding vs authoring"; cut them (~5)
- total ~110 column-lines (~1.2 pages); the rest must come from
  short new sections or dropping one figure/listing pair (4.3, 4.11)
- suggested first batch (pure redundancy): 4.1, 4.2, 4.3, 4.10

# Side notes

- Aug 28 PDF (~/Downloads) matches sections 1-2 of f5a4e7e
- GitHub split diff base f5a4e7e...main, anchor sha256("main.tex")

# Won't do

- 2.15 Git SHA-1 note: impl hardcodes SHA-1 (state.lua tree ids), no
  SHA-256; Git's sha1dc refuses known collisions; user: won't do

- intro: Sybil-resistance caveat sentence (2.2.a) dropped by user
- intro: "a few honest members" vs 3.7.1 "at least one"; kept as is

- abstract "(vs longest chain)": role pairing, kept as is
