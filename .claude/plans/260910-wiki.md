# Goal

- replay Wikipedia edit histories on freechains (open chain)
- FIRST corpus where abuse is PRE-moderation and visible:
  vandalism and its revert are both permanent public revisions
- exercises the REVOKE path end-to-end (no other sim does):
  revoked payloads erased, sweep reclaims, metadata stays

# Profiles (4 pages, one chain each)

- FULL history per article (creation -> today), no sampling;
  LIMIT caps the replay, never the fetch
- polemic: Abortion (13,972 revs)
- event: 2023 Turkey-Syria earthquakes (5,861 revs)
- factual: Photosynthesis (5,514 revs)
- vandal-magnet BLP: George W. Bush (drive-by + fast revert;
  distinct from ideological back-and-forth)
- compare: revert rate, editor mix, reclaimed bytes across
  profiles; vs SE post-moderation downvote 3-12%

# Fetch

- per-page full history via Special:Export
    - 1000 revs per request; loop with offset continuation
- store as `sims/data/wiki/<slug>.xml` (ignored)
- add entries + sha256 to `sims/fetch.sh`

# Mapping (wiki-simple.lua)

- revision -> signed post by editor
    - per-editor keys; IP editors -> shared `anon` key
    - payload = revision text (FILE, like se-simple TMP)
- revert -> `revoke 1000` by the reverter
    - detector: revision sha1 == an earlier revision's sha1
    - revoke each intermediate revision (the vandalism)
    - conservative: misses partial reverts (deterministic)
- ts clamp as in se-simple (per-page ts are monotone; clamp
  guards edge cases)
- env knobs: PAGE/LIMIT/WINDOW/SWEEP/ALIAS/BASE/N_REVOKE

# Pilot: one polemic page first

- [x] probe 26/09/10: Abortion revs 1-2000 (2001->2005-08)
    - revert rate ~15% (300 sha1-revert targets, 195 groups)
    - 42% IP editors (847/2000) -> shared anon key is hot
    - ~18.6 MB / 1000 revs -> full page ~260 MB XML
    - slices at data/wiki/ab{1,2}.xml; export loop works
- [x] LIMIT=1000 smoke 26/09/10: 784 revs + 216 revokes, 150s,
  190 users; revoked payload erased (`get payload` errors),
  metadata still ordered; sweep 44.2 -> 1.8 MB
- [x] full OPEN run 26/09/11: 21,280 ev (13,909 revs + 7,371
  revokes = 53% revoked), 2,134 editors, 3h43m, floor 48 MB
- [x] full GATED run 26/09/11 (BUGGY: revoke 1000 tied the
  welcome like; figures void) -> rerun 26/09/14 (fixed):
  7h28m, 3,518 identities, precision 72%, recidivism 34%,
  community revokes 22% -> wiki/RESULTS.md (A/B section)
- then the other three profiles (event, factual, BLP)

# Gated mode (GATED=true, after the open Abortion run)

- init `--dictator=keys/wp`: gated chain, dictator = platform
  (installed build: dictator is ungated and wins forks)
- per-IP keys (not one shared anon): identities born/die/return
- reps < 500 at post time -> `--beg` + dictator `like 1000`
    - first welcome = birth; re-welcome = RESURRECTION
- revoke gated for editors: reverter < 1000 reps -> dictator
  revokes instead; community/dictator ratio = self-funded
  moderation capacity
- resurrection split (revoke effectiveness in replay):
    - vandal res. (was revoked) = revoke worked, positive
    - innocent res. (drained) = false positive, tpd-21 (d)
    - precision = vandal res. / all res.
    - recidivism = resurrected vandals revoked again
- env: GATED, WELCOME policy (always | never-rewelcome-revoked)
- A/B: same page open vs gated (~2x runtime: reps query + begs)

# Metrics

- as before: ev avg, loose/window, sweep time, packed floor
- new: revert rate per profile, revoked payload bytes
  reclaimed (pack before/after vs SE runs), reverter debt
  (open chain: who could afford revokes if gated)

# Pending (ordered)

- [x] housekeeping 26/09/17: wiki logs -> `sims/wiki/logs/`
  (open, gated, gated-BUGGY, smoke-1k, gated-final-sweep)
- remaining profiles, GATED, sequential (~3h each):
    - event: 2023 Turkey-Syria earthquakes (5,861 revs)
    - factual: Photosynthesis (5,514 revs)
    - BLP: George W. Bush (fetch full history first)
- tighter revert rule on the polemic: restore-previous only
  (kill the sha1 cascade tail); `intervening` + latency cols;
  activity-typed precision (vandal = 0 intervening)
- WELCOME=never rerun: content lost vs 844 recidivists blocked
- Wikipedia Thanks log -> `like <n> member` (author-like)
- upstream question: withhold the 12h refund (500) from a
  revoked post? (one-shot vandal still nets ~+50)
- GitHub corpus: reactions (like/dislike) + minimized comments
  (revoke with payload) -> own plan when started

# Done

- [x] plan + 4 profiles + Special:Export fetch loop
- [x] pilot probe (15% reverts, 42% IP) and 1k smoke
- [x] Abortion OPEN full: 21,280 ev, 53% revoked, 48 MB floor
- [x] Abortion GATED full (fixed): precision 72%, recid 34%,
  community 22%, reps~revoked -0.49
- [x] retracted "revoked posts still mature" (driver bug:
  revoke must outweigh likes; 7,371/7,371 effective now)
- [x] admission blindness 99.4%; latency 34.6% <= 12h;
  intervening 42.4% zero

# Won't do

- whole-wiki dumps (enwiki = TBs; per-page export suffices;
  a small-language wiki dump is the breadth option later)
- partial-revert detection (sha1-identical only)
- talk pages (maybe later: they are the polemic META channel)
- protected-page lifecycle, list pages, dormant stubs
  (extra profiles only if the 4 above leave questions open)
